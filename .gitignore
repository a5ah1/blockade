# Composer — installed into the release zip by build.sh, not committed
/vendor/

# Built release artifacts
*.zip
/build/
/dist/

# Claude Code per-user state (CLAUDE.md itself is kept in git)
/.claude/

# Editor / IDE
.idea/
.vscode/
*.sublime-project
*.sublime-workspace
*.code-workspace
.project
.settings/
nbproject/

# OS cruft
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
desktop.ini

# Logs
*.log
error_log
npm-debug.log*

# Env
.env
.env.local
.env.*.local

# Temp / backup
*.tmp
*.temp
*.cache
*.swp
*.swo
*.bak
*.old
*.backup
*~

# PHPUnit (in case tests are added later)
/phpunit.xml
/.phpunit.result.cache

# Local test harness and working TODO — specific to author's dev setup, not shipped
/ignore/
/TODO.md
